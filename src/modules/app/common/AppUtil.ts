export class AppUtil{
    // endpoints
    private static BASE_URL:string = "https://jsonplaceholder.typicode.com/"
    public static USER_ENDPOINT:string = `${this.BASE_URL}users`
    public static IMAGES_ENDPOINT:string = `${this.BASE_URL}photos`


}